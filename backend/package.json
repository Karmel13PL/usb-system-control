{
  "name": "@usb-system-control/backend",
  "private": true,
  "version": "0.1.0",
  "description": "Backend workspace for USB System Control",
  "workspaces": [
    "auth-service",
    "device-service",
    "agent-service",
    "api-gateway"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "test": "npm run test --workspace auth-service",
    "build": "npm run build --workspace auth-service",
    "dev": "npm run dev --workspace auth-service"
  }
}
