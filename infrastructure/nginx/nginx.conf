events {}

http {
  server {
    listen 80;
    server_name localhost;

    location / {
      return 200 'USB System Control Nginx placeholder';
      add_header Content-Type text/plain;
    }
  }
}
